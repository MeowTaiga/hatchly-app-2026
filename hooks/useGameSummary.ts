/**
 * @deprecated Import from `@/store/GameSummaryProvider` — kept as a re-export
 * so existing `@/hooks/useGameSummary` imports keep working.
 */
export {
  useGameSummary,
  GameSummaryProvider,
  type GameSummary,
} from '@/store/GameSummaryProvider';
