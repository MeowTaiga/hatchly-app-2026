/**
 * Suggestion component registry. Extend by adding new entries.
 * Each id must match backend constants/suggestionTypes.ts
 */
export { SUGGESTION_CONFIG } from './registry';
export type { SuggestionConfig } from './registry';
