/**
 * Game Provider module — game state context and actions.
 *
 * @module GameProvider
 */

export { GameProvider, useGame } from './GameProvider';
export type { GameContextValue, GameState, GameAction } from './types';
