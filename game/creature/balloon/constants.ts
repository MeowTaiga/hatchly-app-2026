/** Sprite size relative to tile (balloons slightly larger than bugs). */
export const BALLOON_SCALE = 1.2;

/** Vertical bob cycle duration (ms). */
export const BALLOON_BOB_CYCLE_MS = 2400;

/** Horizontal drift cycle duration (ms). */
export const BALLOON_DRIFT_CYCLE_MS = 3200;

/** Bob amplitude in pixels (up/down). */
export const BALLOON_BOB_PX = 8;

/** Drift amplitude in pixels (left/right). */
export const BALLOON_DRIFT_PX = 12;
